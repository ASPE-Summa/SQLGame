<UserControl x:Class="SummaSQLGame.Views.Puzzles.MazeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SummaSQLGame.Views.Puzzles"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Maze Puzzle" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding VariantDescription}" FontSize="16" Margin="0,0,0,20" TextWrapping="Wrap"/>
            <TextBlock Text="Voer je SQL-query in:" FontWeight="SemiBold"/>
            <TextBox Text="{Binding QueryText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80"/>
            <Button Content="Controleer antwoord" Command="{Binding CheckAnswerCommand}" Margin="0,10,0,0"/>
            <TextBlock Text="Aantal pogingen: {Binding Attempts}" Margin="0,10,0,0"/>
            <DataGrid ItemsSource="{Binding QueryResult}" AutoGenerateColumns="True" Height="200" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
